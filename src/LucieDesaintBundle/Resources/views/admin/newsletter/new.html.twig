{% extends '@LucieDesaint/admin/index.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">

                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Create" style="border-radius: 20px;" class="btn btn-primary btn-info />
                {{ form_end(form) }}

        </div>
        <div class="row">
            <ul style="background-color: white">
                <li>
                    <a href="{{ path('newsletter_index') }}"  style="border-radius: 20px;" class="btn btn-primary btn-info">Back to the list </a>
                </li>
            </ul>
        </div>
    </div>
    <h1>Newsletter creation</h1>
{% endblock %}
